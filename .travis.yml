language: rust
rust:
  - stable

cache: cargo

before_script:
  - rustup component add rustfmt-preview

script:
  - cargo fmt --all -- --check
  - cargo test --all

notifications:
  email:
    on_failure: change
    on_success: change
